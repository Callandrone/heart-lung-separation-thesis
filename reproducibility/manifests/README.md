# Curated experiment manifests

The original source-selection and split CSVs have not been recovered.
This directory intentionally contains no substitute selections.

When adding original manifests, record their experiment, source, checksum,
column schema, path-relocation rules, and whether they are original or regenerated.
Use stable source IDs and relative paths where possible. Do not include raw
third-party audio here. See ../../REPRODUCIBILITY.md for the outstanding inputs.
